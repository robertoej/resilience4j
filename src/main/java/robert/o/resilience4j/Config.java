package robert.o.resilience4j;

public class Config {
    /**
     * TODO it might be better to assign this value from
     * the config file, where we map resilience4j config names.
     */
    public static final String BACKEND = "backend";
}
